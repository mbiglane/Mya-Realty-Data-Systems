
import React from 'react';

// Admin Panel has been removed.
// The system now uses standard User Authentication (Sign In / Sign Up).

export const AdminPanel: React.FC = () => {
    return null;
};
